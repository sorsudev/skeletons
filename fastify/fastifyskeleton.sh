npm init
npm i bcrypt bookshelf dotenv fastify fastify-cors fastify-multipart fastify-static fastify-swagger fastify-url-data knex md5 nodemailer nodemon pg read-excel-file request

#"scripts": {
   # "dev": "nodemon index.js",
   # "test": "echo \"Error: no test specified\" && exit 1"
#  },
